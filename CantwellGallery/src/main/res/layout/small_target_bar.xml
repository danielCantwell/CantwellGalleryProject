<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

    <TextView
            android:layout_width="wrap_content"
            android:layout_height="20dp"
            android:text="@string/target_bar_header"
            android:id="@+id/targetBarHeader"
            android:layout_centerHorizontal="true"
            android:layout_alignParentTop="true"/>

    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerHorizontal="true"
            android:id="@+id/targetBarCurrentTarget"
            android:layout_above="@+id/targetBarNewTarget"
            android:layout_below="@+id/targetBarHeader"
            android:focusableInTouchMode="false">

        <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/targetBarCurrentTargetImage"
                android:src="@android:drawable/ic_input_get"
                android:layout_centerInParent="true"/>

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="@dimen/header_height"
                android:text="@string/default_directory_title"
                android:id="@+id/targetBarCurrentTargetText"
                android:layout_centerInParent="true"
                android:layout_below="@+id/targetBarCurrentTargetImage"/>

    </RelativeLayout>

    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/target_bar_width"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:id="@+id/targetBarNewTarget">

        <ImageView
                android:layout_width="match_parent"
                android:layout_height="@dimen/new_item_height"
                android:id="@+id/targetBarNewTargetImage"
                android:layout_above="@+id/targetBarNewTargetText"
                android:layout_centerHorizontal="true"
                android:src="@android:drawable/ic_input_get"/>

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="@dimen/header_height"
                android:text="@string/new_item"
                android:id="@+id/targetBarNewTargetText"
                android:layout_alignParentBottom="true"
                android:layout_centerHorizontal="true"/>
    </RelativeLayout>

</RelativeLayout>